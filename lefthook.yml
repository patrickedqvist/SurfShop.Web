pre-commit:
  commands:
    js-lint:
        glob: "*.{ts|tsx}"
        run: yarn eslint --fix {staged_files}
    ts-lint:
        glob: "*.{ts|tsx}"
        run: yarn eslint --fix {staged_files}
    packages-audit:
      run: yarn audit